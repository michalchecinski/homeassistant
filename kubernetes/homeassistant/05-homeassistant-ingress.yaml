apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  namespace: homeassistant
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "0"
  name: homeassistant
spec:
  rules:
  - host: homeassistant.192.168.1.100.nip.io
    http:
      paths:
      - backend:
          serviceName: homeassistant
          servicePort: 8123
        path: /
