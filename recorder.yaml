  purge_keep_days: 5
  db_url: !secret db_url
  include:
    domains:
      - input_boolean
      - input_number
      - input_select
      - switch
      - light
    entities:
      - media_player.lg_webos_smart_tv
  exclude:
    domains:
      - updater
      - automation
      - device_tracker
      - group
      - scene
      - script
    entities:
      - sun.sun
